DATABASE_URL="postgresql://user:password@localhost:5432/climatserv17"

# NextAuth / Magic Link
NEXTAUTH_SECRET="votre-secret-nextauth-generer-avec-openssl"
NEXTAUTH_URL="http://localhost:3000"

# Email Configuration (pour Magic Link et notifications)
# Remplacez ces valeurs en local dans `.env.local` (NE PAS COMMITTER les secrets)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="votre-email@gmail.com"
SMTP_PASSWORD="votre_mot_de_passe_app"
SMTP_FROM="contact@votredomaine.tld"


# Twilio (pour SMS notifications)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_PHONE_NUMBER=""

# Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR="./public/uploads"

# Application
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# Zone de service (codes postaux)
SERVICE_ZONE_CENTER="17000"
SERVICE_ZONE_RADIUS="60"

# Map google
NEXT_PUBLIC_GOOGLE_MAPS_KEY=ta_cle_google

# (SendGrid removed) Utilisez la configuration SMTP ci-dessus pour l'envoi des emails.